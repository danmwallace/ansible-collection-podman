---
- name: Converge
  hosts: molecule
  gather_facts: true
  tasks:
    - name: Apply common role with test values
      ansible.builtin.include_role:
        name: danmwallace.podman.common
      vars:
        common_timezone: "Etc/UTC"
        common_cockpit_enabled: false
        common_users:
          - name: ansible
            groups:
              - wheel
