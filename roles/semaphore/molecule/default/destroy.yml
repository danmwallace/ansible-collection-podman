---
- name: Destroy molecule instance
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Stop and remove semaphore-instance container
      containers.podman.podman_container:
        name: semaphore-instance
        state: absent
