# SPDX-License-Identifier: MIT
---
- name: Destroy molecule instance
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Stop and remove dashy-instance container
      containers.podman.podman_container:
        name: dashy-instance
        state: absent
